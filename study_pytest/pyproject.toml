[project]
name = "study-pytest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "=3.8"
dependencies = [
    "pytest>=8.3.5",
]

[tool.pytest.ini_options]
pythonpath = [
    ".",
]
testpaths = [
    "can_demo",
]
